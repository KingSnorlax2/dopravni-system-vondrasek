/**
 * Main auth configuration export
 * Re-exports from auth.config.ts for convenience
 * 
 * This file can be used as a drop-in replacement for existing auth.ts
 */

export { authOptions } from "@/lib/auth.config"
